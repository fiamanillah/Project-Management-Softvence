info:
  name: Remove Department Manager
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/organization/departments/:id/managers/:managerId"
  headers:
    - name: Authorization
      value: "Bearer {{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
