info:
  name: Get Designation Permissions
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/organization/designations/:id/permissions"
  headers:
    - name: Authorization
      value: "Bearer {{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
