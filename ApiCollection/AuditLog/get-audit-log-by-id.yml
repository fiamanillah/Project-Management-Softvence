info:
  name: Get Audit Log By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/audit-logs/:id"
  headers:
    - name: Authorization
      value: "Bearer {{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
