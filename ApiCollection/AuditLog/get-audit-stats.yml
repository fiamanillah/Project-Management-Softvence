info:
  name: Get Audit Stats
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/audit-logs/stats"
  headers:
    - name: Authorization
      value: "Bearer {{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
