info:
  name: Get Scope Types
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/permissions/scope-types"
  headers:
    - name: Authorization
      value: "Bearer {{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
