info:
  name: Revoke Permission Override
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/users/overrides/:id"
  headers:
    - name: Authorization
      value: "Bearer {{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
