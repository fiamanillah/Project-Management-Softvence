info:
  name: Revoke Delegation
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{baseUrl}}/users/delegations/:id"
  headers:
    - name: Authorization
      value: "Bearer {{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
