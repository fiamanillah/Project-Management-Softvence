info:
  name: Get Delegations
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/users/delegations"
  headers:
    - name: Authorization
      value: "Bearer {{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
