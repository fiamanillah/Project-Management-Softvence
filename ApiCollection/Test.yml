info:
  name: Test
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseurl}}/test"
  auth: inherit

runtime:
  variables:
    - name: baseurl
      value: http://localhost:3030

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
