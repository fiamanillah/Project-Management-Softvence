info:
  name: Change Password
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/auth/change-password"
  body: json
  auth: none

body:json: |-
  {
    "currentPassword": "SuperSecretDevPassword123!",
    "newPassword": "NewSuperSecretPassword123!"
  }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
