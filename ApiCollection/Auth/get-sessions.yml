info:
  name: Get Active Sessions
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/auth/sessions"
  headers:
    - name: Authorization
      value: "Bearer {{accessToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
