info:
  name: Get User Permissions Map
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/auth/permissions"
  headers:
    - name: Authorization
      value: "Bearer {{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
