info:
  name: List Audit Logs
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/audit-logs?page=1&limit=20"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
